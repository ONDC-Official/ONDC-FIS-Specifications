Review Accident Insurance:
  - search: 
    - BAP_TERMS are not present(same for all search requests)

  - on_search_1: 
    - BPP_TERMS are not present(same for all on_search requests)  

  - on_Search_3:
      - In GENERAL_INFO, offer items like Base_price, CONVIENCE_FEE, PROCESSING_FEE, TAX etc are missing.
      - MINIMUM_AGE_LIMIT and other tags structure is incorrect

  - on_select:
       - Item id, price is not matching with on_search & select payload.
       - General Info tags are different form on_search
       - CD_BALANCE tag is required or not(doubt)?
       - KYC_INFO tag details should be moved to on_init call as part of fulfillments.
       - Instead of INSURED_NOMINEE tag use NOMINEE_DETAILS tag. Also use NOMINEE_{{}} for all tags & values should not be present for tags
       - In quote, some fields are missing like, CONVIENCE_FEE, PROCESSING_FEE.
  - init:
    - MASTER_POLICY should not be part of fulfillments( this can be removed from payload).
  
  - on_init payload is missing
  - confirm payload is missing
  - on_confirm:
    - BAP & BPP terms are missing.
    - quote is different from previous calls
  - on_update payload is missing
  


