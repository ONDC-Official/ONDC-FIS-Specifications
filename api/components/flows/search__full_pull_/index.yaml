summary: Search (Full Pull)
steps:
  - summary: Search for schemes
    api: search
    details:
      - description: Search for all mutual fund schemes
    reference: if any
    example:
      value:
        $ref: ../../examples/mutual-funds/search/search_for_mutual_funds_Full_Pull.yaml
  - summary: Respond with the catalog
    api: on_search
    details:
      - description: Returns the schemes on offer
    reference: if any
    example:
      value:
        $ref: ../../examples/mutual-funds/on_search/mutual_funds_catalog.yaml
