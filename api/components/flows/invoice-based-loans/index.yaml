summary: Invoice based loans
details:
  - description: Invoice based loan description
references: if any
steps:
  - summary: Search for Invoice based loan
    api: search
    details:
      - description: User search over the network to avail loan services.
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/search/search-request.yaml"
  - summary: Get a list of all type, loan services from provider
    api: on_search
    details:
      - description: Provide platform sends the catalog of differnt loan types like Invoice based etc.
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/on_search/on_search-request.yaml"

  - summary: Get the Invoice based loan form
    api: form
    deatils:
      description: Provide platform sends the Invoice based form.
    reference: if any
    example:
      value: 
        $ref: ../../examples/invoice-based-loans/forms/invoice-based-loans-org-info.html


  - summary: Selection of a specific service
    api: select
    details:
      - description: The end consumer has selected a specific service and would like to obtain a form for that service.
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/select/select-request-financial-info.yaml"
   #first select form
  - summary: Provider platform provides the form for the specific service.
    api: on_select
    details:
      - description: The provider platform responds with detailed information for the specific service that the consumer would like to avail.
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/on_select/on_select-request-financial-info-form.yaml"      
  - summary: Get the Financial Info form
    api: form
    deatils:
      description: Provider platform sends the Financial Info form.
    reference: if any
    example:
      value: 
        $ref: ../../examples/invoice-based-loans/forms/invoice-based-loans-financial-info.html


   #second select form
  - summary: Selection of a specific service
    api: select
    details:
      - description: The end consumer has selected a specific service and would like to obtain a form for that service.
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/select/select-request-individual-kyc.yaml"
  - summary: Provider platform provides the form for the specific service.
    api: on_select
    details:
      - description: The provider platform responds with detailed information for the specific service that the consumer would like to avail.
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/on_select/on_select-request-individual-kyc-form.yaml" 
   #third select form
  - summary: Get the Individual Kyc form
    api: form
    deatils:
      description: Provide platform sends the Individual Kyc form.
    reference: if any
    example:
      value: 
        $ref: ../../examples/invoice-based-loans/forms/invoice-based-loans-financial-info.html


  - summary: Selection of a specific service
    api: select
    details:
      - description: The end consumer has selected a specific service and would like to obtain a form for that service.
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/select/select-request-org-details.yaml"
  - summary: Provider platform provides the form for the specific service.
    api: on_select
    details:
      - description: The provider platform responds with detailed information for the specific service that the consumer would like to avail.
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/on_select/on_select-request-entity-kyc-form.yaml" 
  - summary: Get the Entity Kyc form
    api: form
    deatils:
      description: Provide platform sends the Entity Kyc form.
    reference: if any
    example:
      value: 
        $ref: ../../examples/invoice-based-loans/forms/invoice-based-loans-account-details.html
                          
  - summary: Consumer platform initializes the request
    api: init
    details:
      - description: Consumer platform shares the terms of order and initializes the order
    reference: if any
    example: 
      value:
        $ref: "../../examples/invoice-based-loans/init/init-request-account-info-complete.yaml"

  - summary: Provider platform accepts/appends the terms of request
    api: on_init
    details:
      - description: Provider platform accepts the terms of request and appends its own terms
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/on_init/on_init-request-bank-account-mandate-form.yaml" 

  - summary: Consumer confirms the request
    api: confirm
    details:
      - description: The consumer platform confirms the request and provides all the required information for confirmation, as per the terms of request
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/confirm/confirm-request.yaml"         
  - summary: Provider platform confirms the order
    api: on_confirm
    details:
      - description: Provider platform confirms the order and provides form details for confirmation
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/on_confirm/on_confirm-request.yaml" 
  - summary: Consumer platform request for latest status
    api: status
    details:
      - description: The consumer platform request the provider platformto provide the latest status
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/status/status-request.yaml" 
  - summary: Provider platforms provides latest order status
    api: on_status
    details:
      - description: Provider platforms provides the updates order status to consumer
    reference: if any
    example:
      value:
        $ref: "../../examples/invoice-based-loans/on_status/on_status-request-after-installment.yaml"                                                
