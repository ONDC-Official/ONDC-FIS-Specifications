summary: SIP cancellation by the investor

references: if any
steps:
  - summary: Cancel
    api: cancel
    details:
      - description: Send cancel request along with cancellation reason
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/cancel/cancel-request.json"
  - summary: On Cancel
    api: on_cancel
    details:
      - description: Send parent order fulfillment cancellation status
    reference: if any
    example:
      value:
        $ref: "../../examples/mutual-funds/on_cancel/on_cancel-sip.json"
